Enhance product module: add product update functionality in repositories and services, refine Livewire components for validation and error handling, update image and variant management logic, and improve Blade views for better user experience and localization.
Add user management module: implement user creation with address dropdown, validation, and dynamic status handling; enhance DataTables for listing, status changes, and deletion via AJAX; add seeder, factory, localization, and Livewire components for better user flow.
Add contact management module: implement migration, model, factory, seeder, repository-service pattern, Livewire components, views for listing and message display; add routes and blade updates for better user interaction.
Add contact reply feature: implement Livewire component, modal for composing replies, and replay logic with service integration; update email layout, contact sidebar, and message display for improved user interaction.
Add home module: implement `HomeController` with index method, update dashboard navigation for dynamic logo display, and integrate required CSS for frontend styling.
Add website layout and update favicon implementation

Introduced initial website layout including home view, header, footer, and related components. Updated the dashboard favicon handling to use dynamic settings instead of static paths for improved flexibility.
Add user profile module: create `UserProfileController`, `SliderRepository`, authentication routes, and user registration/login views with localization support; refactor `User` model and migrations for first/last name fields, update filesystem config with `sliders` disk, and revise website layout.
Refactor home module: remove `HomeController`, introduce `SliderService` for slider management, and add new views for slider creation and display. Implement image upload functionality and integrate DataTables for slider management in the dashboard.
Add slider management functionality: implement `SliderController` for CRUD operations, integrate slider display in the home view, and enhance user registration with terms acceptance. Update localization for slider-related terms and add new sections in the website layout for better product visibility.
Refactor Dashboard services: restructure namespaces to use `Dashboard\Dashboard`, update references across controllers and components, and enhance the `Category` model, migrations, and seeder with an `image` field for category management. Add new category images.
Add category image management: update `CategoryController`, request validation, and views for image upload and display. Extend `CategoryService` with image handling and enhance filesystem configuration with a `categories` disk. Include image column in DataTables.
Add page and FAQ management: create migrations, models, and controllers for pages and FAQ questions. Implement CRUD operations for pages in the dashboard, define scopes and attributes in the `Page` model, and add relevant views for creation, editing, and display. Integrate DataTables for dynamic page management and enhance frontend with a dynamic page view.
Add FAQ Question management: create migration, model, controller, repository, and service for managing FAQ Questions. Integrate DataTables for dynamic management, define routes, and add dashboard views for listing and actions like delete.
Update FAQ module: rename route, controller reference, and sidebar menu to use 'faq-question' instead of 'faqs'. Adjust column naming in FAQ Question view and update AJAX endpoint for data retrieval.
Integrate dynamic FAQ Question submission: add Livewire component with form validation, `FaqQuestion` model, and `createFaqQuestion` functionality in `FaqService`. Update frontend views, localization, and scripts for interactive user feedback.
Refactor homepage and add category and brand listing functionality. Replace `HomeService` with `GlobalService` to consolidate shared services. Update views and routes to dynamically display categories and brands with fallback messages. Add localization for new terms.git
Add product listing functionality for categories and brands. Implement dynamic pages for displaying products by category or brand with pagination support. Update `GlobalService` and add related methods. Refactor views, routes, and controllers to include new functionality and ensure proper localization.
Add product details page functionality: implement `ProductService` with methods for fetching product details, new arrivals, and flash sales. Integrate `GlobalService` updates to expose product-related methods. Refactor homepage with support for new data, update routes, controllers, and views for product sections, and ensure proper localization.
Add homepage product sections: integrate New Arrivals, Flash Sales, and Weekly Deals functionality. Refactor `GlobalService` and controllers to consolidate product fetching logic. Update routes, views, and localization for product sections with proper structuring and dynamic content handling. Optimize flash sale timers and pagination support.
Add product details page: implement product-specific views, controllers, and translation updates. Build dynamic product information section with support for reviews, size selection, and related products. Update `User` model with `reviews` relationship.
Update product links to use `style="all: unset"` for consistent styling across sections.
Generate slugs for products dynamically during creation and updates. Remove `sku` from product creation flow and mark it as auto-generated and readonly. Enhance product description fields with Summernote editor integration.
Add Livewire components for product showcase and wishlist functionality. Implement dynamic variant and stock handling in the product details view. Integrate wishlist toggle actions and display wishlist count in the navbar with real-time updates. Refactor related views, routes, and services to support dynamic product and wishlist features.
Implement wishlist feature: add Livewire components for wishlist table and navbar updates, refactor views to support wishlist actions, and integrate new routes and controllers for seamless functionality.
Implement cart functionality: add migrations, models, and controllers for carts and cart items. Introduce Livewire components for the cart icon and cart table with real-time updates. Refactor views and routes to include cart actions.
Add dynamic cart functionality with product quantity adjustments, variant selection, and discount display. Introduce Livewire components for cart actions and real-time navbar updates. Refactor product details views for enhanced UI and seamless integration with cart and wishlist features.
Implement checkout functionality: add `CheckOutController`, Livewire components for `OrderSummary` and `ShippingDetails`, new views for checkout pages, and update routes for streamlined integration.
Enhance cart and wishlist features: add dynamic quantity updates, real-time cart subtotal calculation, clean cart functionality, and empty state handling. Refactor views for streamlined integration and UI consistency.
Add dynamic location selection to ShippingDetails: implement governorate and city dropdowns, model scope queries, and user data autofill for smoother checkout. Refactor translations and views for UI consistency.
Add coupon functionality: integrate coupon management in `OrderSummary`, update `Cart` and `Coupon` models, enhance Livewire components for validation and real-time updates, and refactor views for coupon application and messaging.
Implement checkout request validation: introduce `CheckOutRequest` for input validation, refactor `CheckOutController` with new `checkout` method, update routes for better grouping, revise `Cart` model to remove `coupon_id`, and enhance `Coupon` handling with code-based validation. Add support for storing coupon data in `Cart` and `Order` tables. Add product uploads to public directory.
Remove unused controllers, requests, and methods; refactor checkout and cart functionalities; introduce `OrderService` for handling orders; update views and translations for consistency; and clean up deprecated code.
Refactor coupon handling across checkout and product views: replace `coupon_id` with `coupon` in `Cart` and related logic, improve `ProductShow` discount calculation, enhance header navigation with route adjustments, and update coupon validation in `OrderService` and `CouponRequest`. Add new product image uploads.
Add Laravel Boost and restructure OrderService: implement `calcInvoicePrice`, `createTransaction`, `clearCart`, and `changeOrderStatus` methods, update coupon and shipping logic, and integrate transaction handling. Include Boost package for enhanced functionality.`